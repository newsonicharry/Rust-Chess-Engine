use crate::chess::board::Board;
use crate::chess::move_ply::MovePly;

pub struct MoveMaker{}

impl MoveMaker {
    
    pub fn make_move(played: MovePly, board: &mut Board){
        
    }
    
    pub fn undo_move(board: &mut Board){
        
    }
    
}